# 数据说明

本项目涉及的真实工业数据（数据中心冷机 / 服务器资源使用等）为私有数据，**不随仓库分发**。

目录约定：
- `data/raw/`：原始数据（本地存放，不上传 GitHub）
- `data/processed/`：清洗、对齐后的中间数据
- `data/features/`：特征工程输出（可选）
- `data/external/`：外部数据（如天气、业务指标、未来扩展的金融市场数据）

出于合规与隐私考虑，Git 仓库中仅保留本 README，不包含任何真实数据文件。
如需复现实验，可使用合成数据或在本地放置脱敏后的数据。
